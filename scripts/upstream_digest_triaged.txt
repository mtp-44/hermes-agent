# Upstream security commits deliberately NOT taken, so upstream_digest.py stops
# re-reporting them. One per line: <upstream sha> <reason>. Commits we landed or
# ported need no entry — their `cherry picked from` / `Ported from upstream`
# trailer already marks them. Remove a line if the reason stops being true
# (e.g. we start using Slack or Docker). Per-commit detail for the 2026-09-24
# entries: docs/cleanup/2026-09-24-security-sync.md and HA-0006.

# 2026-07-22 sync (HA-0002)
9ae17b8ac5 landed: adapted in PR #7 as local 0940b798b (no Ported-from trailer)
16332af60 not applicable: api_server MEDIA data-URL inlining is not in our tree

# 2026-09-24 sync (HA-0006): vulnerable code not in our tree
3966e5de94 not applicable: our async_delegation has no direct state.db writer
56d2438a45 not applicable: no hermes_cli/config_home.py; ensure_hermes_home never skipped on symlinks
5921ba8c06 not applicable: our lifecycle guard is a raw-text regex, wrapper prefixes already caught
6b3a7af73d not applicable: same lifecycle-guard shape as 5921ba8c06
9345c67854 not applicable: per-route webhook toolsets feature (GHSA-2fmg-cjqm-hhrj) absent
940c610994 not applicable: blocklist is defined in tools/environments/local.py itself

# 2026-09-24 sync (HA-0006): out of scope for this deployment
2e08b778ab out of scope: Slack platform not used
2b30744c34 out of scope: photon platform not used
f21332f073 out of scope: CI OSV scan of photon/whatsapp lockfiles
c7fd21add3 out of scope: OpenViking memory provider not used
e1caf88c6c out of scope: Windows-only approval patterns
d966b34cc3 out of scope: Windows-only lifecycle-guard spellings
77ca6a6d12 out of scope: Electron desktop app (GHSA-9f4c-93c8-jc8g) not deployed
fc61608a17 out of scope: Docker terminal backend not used
1aa62ceb45 out of scope: multiplex profiles not enabled
0c74353c86 out of scope: profile isolation, single profile
e70db09f51 out of scope: profile isolation, single profile
c26f75baab out of scope: profile exports into Docker images / CI
3933fdf63b out of scope: pet / provider_media / image-provider features absent
5eeafc8d25 out of scope: OSV preflight caching is a performance change
37e42808e7 out of scope: httplib2 pin in the google-workspace optional skill (no dependency changes)
8fdda828a8 out of scope: npm nanoid override (desktop/web deps; no dependency changes)

# 2026-09-24 triage of the 32 the fixed digest surfaced (HA-0006). The 13 to PORT
# are deliberately absent here so the digest keeps listing them: e4dbb67bf5
# (model-controlled delegate ACP transport — HIGH), a6d9d1d2cf (code_execution
# RPC token), and the dependency/supply-chain group 2b618fe7e5 ea266a6061
# 09da075505 eeab0a251d abcd213504 0945cc5b52 a7efeb0829 e9f64e83c1 88aed1b797
# 43f18cca81 2158f2a5ad.
d83cd6f7c3 out of scope: Azure provider not configured
1f46145e03 not applicable: hardens hermes_cli/urllib_security.py, a post-fork module absent here; PR #7's a061788d4 strips credentials on cross-host redirects in fetch_models
92c2146039 not applicable: urllib_security.py chain, see 1f46145e03
4530a4ca4c not applicable: urllib_security.py chain, see 1f46145e03
27a1042b13 not applicable: urllib_security.py chain, see 1f46145e03
cf34a1e8c7 not applicable: urllib_security.py chain, see 1f46145e03
6e75ba7fa0 not applicable: consolidates redirect credential policy into urllib_security.py; see 1f46145e03
b9b463f3bd deferred: new tool-output risk-scoring feature, not a fix
70449a4939 out of scope: opt-in V2 replay-protected webhook signature; our only route is loopback-bound (127.0.0.1:18099) with one local sender
f7082c5172 out of scope: website/ docs site is neither built nor deployed here
94c27cae60 out of scope: website/ docs site is neither built nor deployed here
a4ffe39f73 out of scope: website/ docs site is neither built nor deployed here
f8d032588c out of scope: pytest is a dev-only test runner, absent from the gateway venv
bb2539ea16 out of scope: pyasn1 is absent from the gateway venv (google-workspace skill dependency)
a3fdc189a4 out of scope: httplib2 is absent from the gateway venv (google-workspace skill dependency)
9bdfa9f36e not applicable: cbor2 absent from the gateway venv; the repo .venv already has 6.1.4
515e88a80f deferred to the next dashboard/TUI release rebuild: JS lockfile pinning only matters when web/ or ui-tui is rebuilt
390a1771b2 deferred to the next dashboard/TUI release rebuild: npm-audit bumps (also edits the undeployed desktop app)
8789cf9f0c deferred to the next dashboard/TUI release rebuild: npm advisories in the root lockfile (photon/whatsapp/website parts out of scope)

# 2026-09-24 (later) triage of the 85 the widened digest surfaced (HA-0008).
# 51 recorded here as not taken. The 34 to PORT are deliberately absent so the
# digest keeps listing them; batches and evidence are in
# docs/cleanup/2026-09-24-security-triage.md.
# -- command detection
7f61872d18 false-positive fix only: stops flagging the safe `git branch -d`
24692ee790 out of scope: IMDS credential fetches; home macOS box, no cloud metadata service
98bf8b2073 not applicable: closes a bypass that d127fb2197's newline masking introduces; HEAD blocks all six witnesses
1c37b9c457 out of scope: approvals.deny inside containers; terminal.backend is local
d41f621071 not applicable: fixes b90dbac1d6's grep scanner / malformed-payload floor, absent here
d127fb2197 false-positive fix only (quoted-newline hardline); would open the bypass 98bf8b2073 closes: port both or neither
da26ff986b out of scope: `rm dir -rf` operand reordering is GNU-only; /bin/rm (BSD) is the only rm on the gateway PATH, verified
0c8bcd3399 false-positive fix only: exempts verifier temp-file cleanup
ebfc49c4d9 false-positive fix only: root-collapse hardline token precision
# -- redaction
93a269c026 not applicable: fixes the rc path/var exemption from 979576d938, absent here
93a0ec705a not applicable: same exemption; HEAD has no exemption and already masks these (probed)
f680431f2e not applicable: redact_for_egress / _BEARER_RESIDUE_RE absent here
848075c501 already present: HEAD's am_ pattern is broader and masks all three test keys (probed)
a8ddb231aa false-positive fix that masks less (shape-gates short values); not wanted
e9d1551e65 ux only: control chars in mask_secret display; leaks nothing
75af6dc57c not applicable: redact_url_credentials strict mode from 34bab1c6ac, absent here
62a00a7391 not applicable: same parent as 75af6dc57c
a573066543 false-positive fix only (os.getenv in JSON/YAML values); depends on 9e872db7d7
9e872db7d7 false-positive fix only (programmatic env lookups); nice-to-have
2b58febe46 out of scope: Fireworks token prefixes; provider not used
# -- approval flow
262825db74 out of scope: smart-approval guardian logging; approvals.mode is manual
151c06e5ec not applicable: _run_approval_gate / plugin approval escalation absent here
9e232a7ff5 not applicable: plugin terminal-backend classification (terminal_env_registry absent); backend is local
3fc1a184f8 not applicable: same as 9e232a7ff5
42e933b411 not applicable: agent/terminal_approval_batch.py absent (follow-up to dd68d17567)
dd68d17567 deferred: new GUI approval batching feature, not a fix
2dfb795cb7 ux only: relabels undelivered CLI prompts deny->cancelled; HEAD stays fail-closed. Attribution chain aac74be2f1..2dfb795cb7: port whole or not at all
1e2cb57973 ux only: /new teardown recorded as deny; fail-closed; attribution chain
6332216384 ux only: withdrawn gateway prompts recorded as deny; fail-closed; attribution chain
05e7e89175 out of scope: loosens unattended contexts to honour allowlists; webhook route is deliver_only, cron disabled
b6b7802447 not applicable: _presence() absent; webhook resolves via unattended_mode first; cron disabled
2a630671d7 out of scope: cron presence-env leak; all Hermes cron jobs disabled (revisit with b6b7802447 if re-enabled)
98a3324821 not applicable: mode=off bypass of _run_approval_gate (absent); computer_use unused
73f8fb74e0 already present: _is_gateway_approval_context excludes webhook sessions (06fa9114b6 port)
349e6611a1 out of scope: smart-approval guardian timeout; approvals.mode is manual
c8c3f4c448 ux only: outcome fields on notify_failed tails; human_wait_window absent
446423d96e not applicable: follow-up to 4341cf1df7
4341cf1df7 not applicable: huge approvals.timeout overflow (ours is 60); human_wait_ceiling / agent/deadline.py absent
f0ffcbc753 not applicable: parity follow-up to 16af3bed8c's denial-breaker addendum
16af3bed8c moot: CLI execute_code lockout from leaked HERMES_EXEC_ASK; the root cause is fixed by porting e37a0321eb
08d9828503 deferred: coalescing identical concurrent prompts is a feature (prerequisite of the attribution chain only)
1596148ff2 out of scope: approvals.single_query_mode for `hermes -q`; nothing here runs -q
aac74be2f1 ux only, HAZARD if ported piecemeal: HEAD's CLI tail blocks only on choice=="deny", so the new "timeout" return would approve. Attribution chain: whole or nothing
f9cd577915 deferred: new /approvals cross-surface mode command, not a fix
a0112ef26e out of scope: smart-approval denial circuit breaker
db90e36202 deferred: `hermes approvals suggest` CLI feature
bd1db5460a out of scope: approvals.smart_policy
d48bf743f2 out of scope: smart-deny owner override (52 files)
b03c94dbed out of scope: observer hooks for smart verdicts
c5e841ab0e behaviour choice, not taken: would cut Telegram/Signal/PWA approval waits from 300s to approvals.timeout (60); upstream later reversed that (a31a31826c)
117f49b7d4 not applicable: plugin escalation gate gateway round-trip; _run_approval_gate absent
